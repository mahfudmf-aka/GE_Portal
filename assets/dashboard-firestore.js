/* R30 — Role & scope aware Ground Experience dashboard. Firestore/GEStore only. */
(function(){
'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=n=>new Intl.NumberFormat('id-ID').format(Number(n||0));
const pct=n=>`${Math.max(0,Math.min(100,Math.round(Number(n||0))))}%`;
const link=(label,href,primary=false)=>`<a class="ge-dash-action ${primary?'primary':''}" href="${href}">${esc(label)}</a>`;
const panel=(title,body,more)=>`<section class="ge-dash-panel"><div class="ge-dash-panel-head"><h2>${esc(title)}</h2>${more?`<a class="ge-link" href="${more.href}">${esc(more.label)}</a>`:''}</div>${body}</section>`;
const kpi=(label,value,sub,href)=>`<a class="ge-dash-kpi" href="${href||'#'}"><div class="ge-dash-kpi-icon" aria-hidden="true"></div><div><small>${esc(label)}</small><b>${esc(value)}</b><span>${esc(sub||'')}</span></div></a>`;
const empty=t=>`<div class="ge-dash-empty">${esc(t)}</div>`;
function session(){return window.gxGetSession?.()||window.GX_CURRENT_USER||{}}
function pov(s){return window.GXDashboardPOV?.(s)||'unresolved'}
function stations(s){return String(s.scopeType||'').toUpperCase()==='ALL'?[]:(Array.isArray(s.airports)?s.airports.map(x=>String(x).toUpperCase()):[])}
function stationValues(x){const r=x?.stations??x?.stationCodes??x?.stationCode??x?.airport??x?.station??x?.relatedAirport??[];return (Array.isArray(r)?r:String(r||'').split(/[,;]+/)).map(v=>String(v).trim().toUpperCase()).filter(Boolean)}
function scoped(rows,s){const a=stations(s);if(!a.length)return rows||[];const set=new Set(a);return (rows||[]).filter(x=>{const xs=stationValues(x);return !xs.length||xs.some(v=>set.has(v))})}
function progress(i){const real=Number(i.real??i.progress??0),plan=Number(i.plan??i.target??100);return plan>0?Math.min(100,(real/plan)*100):real}
function initiativeRows(rows){return (rows||[]).slice().sort((a,b)=>String(a.dueDate||a.endDate||'9999').localeCompare(String(b.dueDate||b.endDate||'9999'))).slice(0,6).map(i=>`<div class="ge-dash-list-row"><div><b>${esc(i.name||i.title||'Initiative')}</b><small>${esc(stationValues(i).join(', ')||'All scope')} • PIC ${esc(i.pic||'-')} • Due ${esc(i.dueDate||i.endDate||'-')}</small></div><strong>${pct(progress(i))}</strong></div>`).join('')}
function serviceRows(rows){return (rows||[]).slice(0,6).map(x=>`<div class="ge-dash-list-row"><div><b>${esc(x.name||x.provider||x.vendor||x.service||'Service')}</b><small>${esc(stationValues(x).join(', ')||x.scope||'-')}</small></div><span class="ge-dash-status ${/expired|inactive/i.test(String(x.status||''))?'warn':'good'}">${esc(x.status||'Active')}</span></div>`).join('')}
function titleFor(p,s){if(p==='superadmin')return['System / Governance','Super Admin / System Dashboard','System governance, access, data health, and enterprise administration.'];if(p==='admin')return['Ground Experience Administration','Ground Experience Admin Dashboard','Operational administration, master data, users, initiatives, and Ground Experience network.'];if(p==='management')return['Management Overview','Management Dashboard','Executive view of experience, progress, readiness, and items requiring attention.'];if(p==='ge-team')return['Ground Experience / Head Office','Head Office Dashboard','Operational monitoring and follow-up across assigned stations and Ground Experience scope.'];if(p==='branch')return['Branch Office','Branch Office Dashboard',`Operational view for ${stations(s).join(', ')||s.unit||'assigned station'}.`];return['Ground Experience','Dashboard','Role-based operational overview.']}
function collectionsFor(p){const common=['initiatives','projectEvents','airports','lounges','serviceProcurement','assets','facilities','stationMaterials','touchpointStandards'];if(p==='superadmin')return [...common,'users','auditLogs'];if(p==='admin')return [...common,'users'];return common}
function render(){const root=document.getElementById('dashboardRoot');if(!root)return;const s=session(),p=pov(s),d=window.GEStore.get();const ins=scoped(d.initiatives,s),aps=scoped(d.airports,s),lounges=scoped(d.lounges,s),gha=scoped(d.serviceProcurement,s),assets=scoped(d.assets,s),facilities=scoped(d.facilities,s),events=scoped(d.projectEvents||d.events,s),standards=scoped(d.touchpointStandards,s),users=d.users||[],audit=d.auditLogs||[];const activeIns=ins.filter(x=>!/complete|closed|done/i.test(String(x.status||''))).length;const overdue=ins.filter(x=>{const due=x.dueDate||x.endDate;if(!due)return false;return new Date(due)<new Date()&&!/complete|closed|done/i.test(String(x.status||''))}).length;const [eyebrow,title,desc]=titleFor(p,s);const scope=stations(s);let admin='';if(p==='superadmin')admin=`<div class="ge-dash-grid ge-dash-grid-3">${panel('System & Portal Status',`<div class="ge-dash-status-grid"><div><small>Business source</small><span class="ge-dash-status good">Firestore</span></div><div><small>Users</small><b>${fmt(users.length)}</b></div><div><small>Audit activity</small><b>${fmt(audit.length)}</b></div></div>`,{href:'app.html?page=portal-management',label:'Portal Management'})}${panel('User & Access Governance',`<div class="ge-dash-count-grid"><div><b>${fmt(users.length)}</b><small>Total Users</small></div><div><b>${fmt(users.filter(x=>String(x.status||'Active')==='Active').length)}</b><small>Active</small></div></div>`,{href:'app.html?page=admin',label:'Manage Users'})}${panel('Super Admin Tools',`<div class="ge-dash-action-list">${link('Portal Management','app.html?page=portal-management')}${link('Audit Log','app.html?page=audit-log')}${link('Master Data & Partners','app.html?page=master-data')}</div>`)}</div>`;
if(p==='admin')admin=`<div class="ge-dash-grid ge-dash-grid-3">${panel('Ground Experience Administration',`<div class="ge-dash-action-list">${link('Manage Users','app.html?page=admin')}${link('Master Data & Partners','app.html?page=master-data')}${link('Data Management','app.html?page=data')}</div>`) }${panel('Account Overview',`<div class="ge-dash-count-grid"><div><b>${fmt(users.length)}</b><small>Users</small></div><div><b>${fmt(users.filter(x=>String(x.status||'Active')==='Active').length)}</b><small>Active</small></div></div>`,{href:'app.html?page=admin',label:'User & Access'})}${panel('Partner / Provider Data',`<div class="ge-dash-count-grid"><div><b>${fmt(lounges.length)}</b><small>Lounge / Tenant</small></div><div><b>${fmt(gha.length)}</b><small>GHA / Provider</small></div></div>`,{href:'app.html?page=master-data',label:'Master Data'})}</div>`;
root.innerHTML=`<div class="ge-dash-head"><div><span class="ge-dash-eyebrow">${esc(eyebrow)}</span><h1>${esc(title)}</h1><p>${esc(desc)} <span class="ge-dash-context">${scope.length?`Scope: ${esc(scope.join(', '))}`:'Enterprise / assigned scope'}</span></p></div></div><section class="ge-dash-kpis">${kpi('Stations',fmt(aps.length),'Airport / station scope','app.html?page=network-stations')}${kpi('Active Initiatives',fmt(activeIns),'Current initiatives','app.html?page=inisiatif')}${kpi('Overdue / Attention',fmt(overdue),'Initiatives requiring follow-up','app.html?page=calendar')}${kpi('Lounge / Tenant',fmt(lounges.length),'Service providers','app.html?page=lounge-list')}${kpi('GHA / Providers',fmt(gha.length),'Ground service providers','app.html?page=master-data')}${kpi('Assets & Facilities',fmt(assets.length+facilities.length),'Recorded resources','app.html?page=asset-facility')}</section>${admin}<div class="ge-dash-grid ge-dash-grid-3">${panel(p==='management'?'Strategic Initiative Progress':'Initiative & Action',initiativeRows(ins)||empty('No initiatives in this scope.'),{href:'app.html?page=inisiatif',label:'Open Initiatives'})}${panel('Calendar & Project Tracking',`<div class="ge-dash-count-grid"><div><b>${fmt(events.length)}</b><small>Activities</small></div><div><b>${fmt(overdue)}</b><small>Need Attention</small></div></div>`,{href:'app.html?page=calendar',label:'Open Calendar'})}${panel('Service / Provider Status',serviceRows([...lounges,...gha])||empty('No provider records in this scope.'),{href:'app.html?page=network-stations',label:'Airport Experience'})}</div><div class="ge-dash-grid ge-dash-grid-3">${panel('Readiness & Standards',`<div class="ge-dash-count-grid"><div><b>${fmt(standards.length)}</b><small>Standard records</small></div><div><b>${fmt(aps.length)}</b><small>Stations monitored</small></div></div>`,{href:'app.html?page=standar',label:'Readiness & Standards'})}${panel('Asset & Facility',`<div class="ge-dash-count-grid"><div><b>${fmt(assets.length)}</b><small>Assets</small></div><div><b>${fmt(facilities.length)}</b><small>Facilities</small></div></div>`,{href:'app.html?page=asset-facility',label:'Open Assets'})}${panel('Quick Access',`<div class="ge-dash-action-list">${link('Airport Experience','app.html?page=network-stations')}${link('Initiative','app.html?page=inisiatif')}${link('Calendar / Gantt','app.html?page=calendar')}${p==='branch'?link('Station Profile / 360','app.html?page=station-360'):link('Planning Workspace','app.html?page=planning-workspace')}</div>`)}</div>`;root.dataset.dashboardPov=p;}
async function boot(){const root=document.getElementById('dashboardRoot');try{await window.GEStore.waitAuth();const s=session(),p=pov(s);await window.GEStore.hydrate(collectionsFor(p));render()}catch(e){console.error('[R30 dashboard]',e);if(root)root.innerHTML=`<section class="ge-dash-panel"><h2>Dashboard belum dapat dimuat</h2><p>${esc(e?.message||'Data source unavailable.')}</p></section>`}}
window.addEventListener('gx-data-background-refresh',()=>{try{render()}catch(e){}});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
